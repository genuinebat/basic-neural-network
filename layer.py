import math

class HLayer():
	def __init__(self, _n_in, _n_out, _weights, _biases):
		self.n_in = _n_in 
		self.n_out = _n_out
		self.weights = _weights
		self.biases = _biases
	
	def forward(self):
		pass	
